#include <bits/stdc++.h>
using namespace std;
int main()
{
    enum Gender {
        Male,  Female 
    };
    enum Color { 
        Red, Green 
    };
    Gender gender = Male;
    Color color = Red;
    if (gender == Gender::Male && color == Color::Red)
    cout << "Equal";


    enum Gender2 {
        Other, NonBinary 
    };
    Gender2 gender2 = NonBinary;
    cout << "\n" << gender << "\n" << gender2 << "\n";
    int otherVariable = 10;

    cout  <<gender << "\n";
    return 0;
}
