#include <bits/stdc++.h>
using namespace std;
int main()
{
    nullptr_t np1, np2;
    if (np1 == np2)
        cout << "\ncan compare" ;
    else
        cout << "\ncan not compare";
    char *x = np1;  
    
    if (x == nullptr)
        cout << "\nx is null";
    else
        cout << "\nx is not null" ;
 
    return 0;
}
