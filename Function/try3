#include <iostream>
using namespace std;

int main()
{
    try  {
       throw 'a';
    }
    catch (char x)  {  // Catch as a character
        cout << "Caught " << x << endl;
    }
    return 0;
}

