
// Example:
 
#include<iostream>
using namespace std;
 
template<class T>
T getMax(T a,T b)
{
    T result;
    result=(a>b)?a:b;
    return result;
}
 
main()
{
  int a=getMax(2,5);
  double d=getMax(2.6,7.6);
  cout<<endl<<a;
  cout<<endl<<d;
    return 0;
}
