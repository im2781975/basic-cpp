#include<iostream>
using namespace std;
template<typename T>
void printList(initializer_list<T> text)
{
    for (const auto & value: text)
        cout << value << " ";
}
int main()
{
    printList({"One","Two","Three"} );
    return 0;
}
