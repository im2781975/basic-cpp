You are given a list of strings. You need to output for each string the previous index where it appeared. If it didn’t occur previously then output -1. Use STL Map for this problem.	
#include <iostream>
#include <string>
#include <map>
using namespace std;

int main() {
    map<string, int> lastIndex;
    int n;
    cin >> n;
    string s;
    
    for (int i = 0; i < n; i++) {
        cin >> s;
        if (lastIndex.find(s) == lastIndex.end()) {
            // If the string is not in the map, output -1.
            cout << -1 << endl;
        } else {
            // Output the last index where the string appeared.
            cout << lastIndex[s] << endl;
        }
        // Update the last index of the string.
        lastIndex[s] = i;
    }
    
    return 0;
}

