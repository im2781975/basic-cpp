#include<bits/stdc++.h>
using namespace std;
int main()
{
    unordered_map<int,int>um;
    um.insert(make_pair(1,2));
    um.insert(make_pair(4,9));
    um.insert(make_pair(6,5));
    um.insert(make_pair(7,0));
    um.insert(make_pair(9,2));
    unordered_map<int,int>::iterator it;
    for(it=um.begin();it!=um.end();it++)
    {
        cout<<it->first <<" "<<it->second<<"\n";
    }
    
}
 
