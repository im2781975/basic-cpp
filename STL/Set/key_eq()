#include <iostream> 
#include <string> 
#include <unordered_set> 
using namespace std; 
int main()
{
    unordered_set<string> ms; 
    
    bool res=ms.key_eq()("a","A"); 
    cout << "ms.key_eq() is ";
    
    if (res == 1) 
    cout << "case insensitive"; 
    
    else
        cout << "case sensitive"; 
    
} 
