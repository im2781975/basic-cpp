#include <iostream>
#include<list>
using namespace std;
int main()
{
    list<int>v{ 1, 2, 3, 4, 5, 3};

    list<int>::iterator i1;
    list<int>::iterator i2;

    i1 = v.begin();
    i2 = v.end();
 
    if (i1!=i2) {
        cout << "Yes";
    }
 
    int count = distance(i1,i2);
 
    cout << "\ncount = " << count;
    return 0;
}
