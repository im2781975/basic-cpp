#include <iostream>
using namespace std;
int main()
{
    int num = 10;
    
    int* ptr = NULL;
    ptr = &num;
    if (ptr == NULL) {
        cout << "No value is pointed";
    }
    else {
        cout << "The value pointed is " << *ptr;
    }
    return 0;
}
